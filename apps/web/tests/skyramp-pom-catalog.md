<!-- skyramp-pom-catalog: auto-generated by skyramp_reuse_code -->

## createUsersFixture (fixtures/users.ts)
- **Import:** `../../playwright/fixtures/users` (relative to `apps/web/playwright/`)
- **Iframe:** top-level page
- **Note:** Not a class-based POM — a Playwright fixture factory (`test.extend`-style) requiring the repo's custom `test` from `./lib/fixtures`, which is incompatible with the Skyramp-generated test's required `@skyramp/skyramp` runtime (`newSkyrampPlaywrightPage`). Cannot be substituted into a Skyramp-generated spec without breaking the mandated runtime imports.
- **Methods:**
  - `create(opts)` — creates a DB-backed user via Prisma, returns a user handle
  - `apiLogin(navigateToUrl?, password?)` — logs a created user in via API/session (no UI form fill); used by all repo e2e specs instead of manually filling `#email`/`#password`
  - `login(password?)` — deprecated, UI-form login
  - `deleteAll()` / `delete(id)` / `deleteByEmail(email)` — teardown

## createEmailsFixture (fixtures/emails.ts)
<!-- depth: signature -->
- **Import:** `../../playwright/fixtures/emails`
- **Methods:** `createEmailsFixture()` — mailhog-based email assertions fixture (unrelated to login/UI locators)

## types (fixtures/types.ts)
<!-- depth: signature -->
- **Import:** `../../playwright/fixtures/types`
- **Properties:** `TimeZoneEnum` — enum, unrelated to this test's actions

## event-types-listing-view.tsx (apps/web/modules/event-types/views/event-types-listing-view.tsx)
- **Note:** This is the application SOURCE file being tested (owns the `data-testid="new-event-type"` / `title="Create a new event type"` button), not test infrastructure. Nothing to reuse here — it is the target under test, not a POM.

## testUtils (playwright/lib/testUtils.ts)
- **Import:** `../lib/testUtils` (relative to `apps/web/playwright/`)
- **Iframe:** top-level page
- **Functions (take plain `page: Page`, compatible with Skyramp runtime):**
  - `createNewUserEventType(page, args: { eventTitle, username? })` — clicks `[data-testid=new-event-type]` then fills/saves the create-event-type dialog. **Action mismatch**: this test intentionally only hovers/inspects the button to verify its `title` tooltip attribute and must NOT click it (clicking would open the create dialog, a different flow). Not substitutable.
  - `gotoFirstEventType(page)` — navigates into an existing event type's edit page; not used by this test
  - `saveEventType(page)` — saves the event-type edit form; not used by this test
  - `gotoBookingPage(page)` / `bookTimeSlot(...)` / `selectFirstAvailableTimeSlotNextMonth(...)` — booking flow helpers; unrelated
  - `submitAndWaitForResponse(...)` — generic form-submit + network-correlation helper; no submit action in this test
  - No helper exists for dismissing the "Want to update your timezone?" dialog captured in the recorded trace.

## fixtures.ts (playwright/lib/fixtures.ts)
<!-- depth: signature -->
- **Import:** `../lib/fixtures`
- **Note:** Defines the repo's custom `test` (fixture-extended Playwright test object: `page`, `users`, `bookings`, `eventTypePage`, etc.). Same incompatibility as `createUsersFixture` above — requires swapping the Skyramp-mandated `test`/`expect`/`newSkyrampPlaywrightPage` imports, which is a hard failure per the runtime-preservation rule.

## oauth-provider.e2e.ts / oauth-client-helpers.ts / oauth-client-owner-crud.e2e.ts
<!-- depth: signature -->
- OAuth-provider/client login helpers (`#email`/`#password` form fill for OAuth consent screens). Different flow (OAuth client authorization), different page context from `/event-types`. No selector or action overlap with this test's hover/tooltip assertion.
